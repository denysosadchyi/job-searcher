# Changelog - Autonomous Improvements

This log is maintained automatically by Claude Code during nightly runs at 3:00 AM.
Each entry describes what was analyzed, decided, implemented, and verified.

---

## 2026-03-22 12:38

### Видалення дублікатів вакансій та захист від повторного додавання

**Проблема:** У файлі vacancies.md накопичилось 12 дублікатів URL — переважно у секціях Work.ua (10 дублів) та DOU.ua (2 дублі). Це засмічувало інтерфейс і витрачало зайві запити до Groq API на аналіз вже існуючих вакансій. Причина — функція `add_vacancy_to_md` в check_new.py не перевіряла, чи URL вже є у файлі.

**Рішення:** 1) Написано скрипт для автоматичного видалення 12 дублікатів з vacancies.md (файл зменшився з 105 до 93 рядків). 2) Додано захист від дублікатів у функцію `add_vacancy_to_md` — перед додаванням нового запису перевіряється наявність URL у всьому файлі. Якщо URL вже є, запис пропускається з повідомленням у лог.

**Змінені файли:** vacancies.md, check_new.py

**Перевірка:** Імпорт app.py та check_new.py — без помилок. Тест дедуплікації: спроба додати існуючий URL коректно блокується з повідомленням "Skipping duplicate URL". Повторна перевірка vacancies.md — 0 дублікатів.

---

## 2026-03-23 03:03

### Додано сортування та пошук вакансій

**Проблема:** Список вакансій зріс до 70+ позицій, але не було можливості швидко знайти потрібну вакансію за назвою чи відсортувати за релевантністю. Користувач мусив скролити весь список вручну.

**Рішення:** Додано тулбар з двома інструментами: 1) **Сортування** — три кнопки: Default (порядок з файлу), Score ↓ (за оцінкою від найвищої), Date ↓ (за датою від найновішої). Сортування працює всередині кожної секції. 2) **Пошук** — текстове поле для миттєвого фільтрування вакансій за назвою, типом компанії, аналізом, зарплатою та форматом роботи. Стилі інтегровані в існуючу дизайн-систему (dark theme, відповідні кольори та розміри).

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна, JavaScript функції sortItems(), matchesSearch(), parseDate(), setSort(), onSearch() коректно інтегровані з існуючою логікою рендерингу та фільтрації.

---

## 2026-03-24 03:03

### Додано панель статистики вакансій

**Проблема:** Зі зростанням кількості вакансій (70+) користувач не мав швидкого огляду загальної картини — скільки вакансій є по джерелах, який середній бал, як розподіляються оцінки. Доводилось скролити весь список або вручну підраховувати.

**Рішення:** Додано згортувану панель "Статистика" під заголовком сторінки з чотирма картками: 1) **Всього вакансій** — загальна кількість. 2) **Середній бал** — середнє значення оцінок з 10. 3) **Розподіл оцінок** — візуальний прогрес-бар з кольоровими сегментами (Hot/OK/Low/N/A) та легендою з кількостями. 4) **Джерела** — чіпи з назвами джерел та кількістю вакансій з кожного, відсортовані за спаданням. Панель за замовчуванням згорнута щоб не займати місце, розгортається одним кліком. Статистика автоматично оновлюється при кожному оновленні даних. На десктопі використовується 3-колонковий grid, на мобільних — 2-колонковий.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (перевірено парсером). CSS стилі інтегровані в існуючу дизайн-систему. JavaScript функції renderStats(), toggleStats() коректно працюють з існуючими даними allData та фільтром SKIP.

---

## 2026-03-25 03:03

### Додано кнопки «Позначити всі як переглянуті» та «Розгорнути/Згорнути всі»

**Проблема:** Коли за ніч накопичувалось 10-20 нових вакансій, користувач мусив клікати на кожну картку окремо, щоб зняти позначку "new". Також не було швидкого способу переглянути аналіз усіх вакансій одразу — треба було розгортати кожну картку по черзі.

**Рішення:** Додано панель дій (actions row) під тулбаром з двома кнопками: 1) **«✓ Позначити всі як переглянуті»** — одним кліком позначає всі вакансії як переглянуті, прибираючи "new" бейджі. Кнопка автоматично ховається, коли немає нових вакансій (щоб не займати місце). Після кожного перегляду картки або завантаження даних перевіряється наявність нових вакансій для оновлення видимості кнопки. 2) **«↕ Розгорнути всі / Згорнути всі»** — toggle-кнопка для масового розгортання/згортання всіх карток з аналізами. Зручно для швидкого перегляду всіх аналізів на одній сторінці.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (1 script open/close). Усі нові JS-функції (markAllSeen, updateMarkAllSeenBtn, toggleExpandAll) присутні та коректно інтегровані з існуючою логікою seenUrls, openCards, renderFilters.

---

## 2026-03-26 03:03

### Додано функцію «Обране» (★ Favorites) для збереження цікавих вакансій

**Проблема:** При активному пошуку роботи серед 80+ вакансій не було способу позначити ті, що зацікавили — доводилось запам'ятовувати або записувати окремо. Після перегляду списку неможливо було швидко повернутись до вакансій, на які хочеш відгукнутись.

**Рішення:** Додано повноцінну систему «Обране» (Favorites): 1) **Кнопка ★/☆ на кожній картці** — натискання додає/прибирає вакансію з обраного. Зірочка підсвічується жовтим кольором для обраних вакансій. 2) **Фільтр «★ Fav»** — нова кнопка в панелі фільтрів показує тільки обрані вакансії з лічильником. 3) **Тег «★ fav»** — жовтий бейдж на картці для швидкої візуальної ідентифікації обраних. 4) **Статистика** — у панелі статистики додано картку «В обраному» з кількістю обраних вакансій. Дані зберігаються в localStorage (ключ `favUrls`), тому обране не зникає між сесіями.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (23944 символів, 1 script open/close). JS-функції getFavUrls(), toggleFav(), isFav() коректно інтегровані з існуючою логікою фільтрації (passFilter), рендерингу (render, renderFilters) та статистики (renderStats).

---

## 2026-03-27 03:03

### Додано клавіатурну навігацію для швидкого перегляду вакансій

**Проблема:** При перегляді 80+ вакансій користувач мусив клікати мишкою на кожну картку — розгортати, закривати, переходити до наступної. Це повільно, особливо при щоденному перегляді нових вакансій. Для досвідченого користувача відсутність клавіатурних скорочень — суттєвий мінус UX.

**Рішення:** Додано повноцінну систему клавіатурної навігації: 1) **j/↓ та k/↑** — переміщення фокусу між картками вакансій з плавним скролом до обраної картки. Активна картка підсвічується фіолетовою рамкою (accent color glow). 2) **Enter** — розгортання/згортання картки для перегляду аналізу. 3) **o** — відкриття посилання вакансії у новій вкладці. 4) **f** — додавання/видалення вакансії з обраного. 5) **x** — видалення вакансії (з підтвердженням). 6) **?** — модальне вікно з довідкою по всіх скороченнях. 7) **Esc** — закриття довідки або зняття фокусу з картки. Клавіатура не перехоплюється під час вводу в поле пошуку. У статус-барі додано кнопку «⌨ ?» для швидкого виклику довідки мишкою.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (28596 символів, 1 script open/close). JS-функції getVisibleCards(), setFocus(), showKbHelp() та обробник keydown коректно інтегровані з існуючими функціями toggle(), toggleFav(), del().

---

## 2026-03-28 03:03

### Покращено рендеринг markdown у Changelog та виправлено баг зі статус-баром

**Проблема:** Сторінка changelog.html відображала записи як сирий текст — `**Проблема:**`, `**Рішення:**`, `` `функція()` `` показувались з зірочками та лапками замість форматування. Це ускладнювало читання. Також у index.html функція `status()` при оновленні стану з'єднання затирала кнопку `⌨ ?` для виклику клавіатурних скорочень, бо перезаписувала весь innerHTML статус-бару.

**Рішення:** 1) У changelog.html додано повноцінний рендеринг inline-markdown: `**bold**` → жирний текст (кольором accent), `` `code` `` → моноширинний блок з підсвіткою, `*italic*` → курсив. 2) Додано спеціальну стилізацію для секцій «Змінені файли» (відокремлена верхньою рамкою, приглушений колір) та «Перевірка» (зелений колір, зменшений шрифт) — це робить структуру записів візуально чіткішою. 3) У index.html виправлено функцію `status()` — тепер вона зберігає кнопку клавіатурних скорочень при кожному оновленні стану.

**Змінені файли:** changelog.html, index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура обох файлів валідна (changelog.html: 4557 символів, 1 script open/close; index.html: 28762 символів, 1 script open/close). Функції renderMd(), renderLine() коректно обробляють markdown-форматування та спеціальні секції.

---

## 2026-03-29 03:03

### Додано трекінг статусу відгуків (Application Pipeline)

**Проблема:** При активному пошуку роботи серед 100+ вакансій не було способу відстежувати, на які вакансії вже відгукнувся, де проходить інтерв'ю, а де отримано офер чи відмову. Доводилось вести окремий список в блокноті або таблиці. Система «Обране» дозволяла лише зберегти цікаві вакансії, але не відслідковувати етап відгуку.

**Рішення:** Додано повноцінну систему трекінгу application pipeline: 1) **Дропдаун статусу на кожній картці** — вибір із 5 станів: «—» (без статусу), «Applied», «Interview», «Offer», «Rejected». Дропдаун розміщений поруч з кнопками обраного та видалення. 2) **Кольорові теги статусу** — Applied (фіолетовий), Interview (зелений), Offer (яскраво-зелений, жирний), Rejected (приглушений, закреслений). 3) **Фільтр «Pipeline»** — нова кнопка в панелі фільтрів показує всі вакансії, що мають будь-який статус відгуку, з лічильником. 4) **Статистика Pipeline** — у панелі статистики додано картку з розбивкою по статусах (Applied/Interview/Offer/Rejected). 5) **Клавіша `s`** — швидке перемикання статусу з клавіатури (циклічно: — → Applied → Interview → Offer → Rejected → —). Довідку оновлено. Дані зберігаються в localStorage (ключ `vacancyStatuses`).

**Змінені файли:** index.html, vacancies.md (очищено зайві порожні рядки)

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (32735 символів, 1 script open/close). JS-функції getStatuses(), setStatus(), getStatus(), statusTag() коректно інтегровані з фільтрацією (passFilter), рендерингом (render, renderFilters) та статистикою (renderStats). Клавіатурне скорочення `s` додано до обробника keydown.

---

## 2026-03-30 03:03

### Додано нотатки до вакансій для збереження думок та контактів

**Проблема:** При активному пошуку роботи серед 100+ вакансій не було способу записати свої думки, враження від перегляду, контакти рекрутерів або дати інтерв'ю прямо біля вакансії. Доводилось вести окремий блокнот або таблицю, постійно перемикаючись між вкладками.

**Рішення:** Додано повноцінну систему нотаток до вакансій: 1) **Кнопка ✎ на кожній картці** — відкриває текстове поле для введення нотатки. Кнопка підсвічується фіолетовим кольором, якщо нотатка вже є. 2) **Область нотатки в розгорнутій картці** — під аналізом відображається нотатка або кнопка «Додати». Редагування через inline-textarea зі збереженням по кнопці або `Ctrl+Enter`. 3) **Тег «✎ note»** — фіолетовий бейдж на картці для швидкої ідентифікації вакансій з нотатками. 4) **Фільтр «✎ Notes»** — нова кнопка в панелі фільтрів для перегляду лише вакансій з нотатками. 5) **Пошук по нотатках** — текст нотаток включено до пошукової індексації. 6) **Клавіша `n`** — швидке відкриття редактора нотаток з клавіатури. 7) **Статистика** — у панелі статистики додано картку «З нотатками» з лічильником. Дані зберігаються в localStorage (ключ `vacancyNotes`).

**Змінені файли:** index.html, vacancies.md (очищено зайві порожні рядки)

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (38120 символів, 1 script open/close). JS-функції getNotes(), getNote(), saveNote(), hasNote(), startEditNote(), saveNoteFromInput() коректно інтегровані з фільтрацією (passFilter), рендерингом (render, renderFilters), пошуком (matchesSearch) та статистикою (renderStats). Клавіатурне скорочення `n` додано до обробника keydown та довідки.

---

## 2026-03-31 03:03

### Додано експорт вакансій у CSV для роботи з таблицями

**Проблема:** При активному пошуку роботи серед 100+ вакансій не було способу вивантажити дані для аналізу в Google Sheets чи Excel. Користувач не міг поділитись прогресом пошуку з менторами або зберегти резервну копію свого трекінгу (обрані, статуси pipeline, нотатки).

**Рішення:** Додано кнопку «📥 Експорт CSV» в панелі дій. Функція `exportCSV()` збирає всі вакансії з усіх джерел і формує повноцінний CSV-файл з 12 колонками: Source, Title, URL, Score, Type, Salary, Remote, Date, Status, Pipeline, Favorite, Note. Файл включає: 1) Дані з API (назва, URL, оцінка, тип компанії, зарплата, формат роботи, дата). 2) Локальні дані користувача з localStorage (статус pipeline, обране, нотатки). CSV коректно екранує лапки та коми у даних, додає BOM-маркер (`\uFEFF`) для правильного відкриття кирилиці в Excel, та автоматично завантажується як файл `vacancies-YYYY-MM-DD.csv`. Також очищено зайві порожні рядки наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (39501 символів, 1 script open/close). JS-функція exportCSV() коректно інтегрована — використовує існуючі allData, getStatuses(), getFavUrls(), getNotes() для збирання даних. CSV генерується з правильним екрануванням та BOM для сумісності з Excel/Google Sheets.

---

## 2026-04-01 03:03

### Додано компактний список-вигляд для швидкого перегляду вакансій

**Проблема:** При 100+ вакансіях в базі перегляд у режимі карток вимагає багато скролінгу. Кожна картка займає значне місце на екрані, що ускладнює швидке сканування списку. Не було можливості перемкнутися на більш щільний формат відображення.

**Рішення:** Додано перемикач виглядів (Card/List) у тулбар: 1) **Режим «▦ Card»** — звичайний режим карток (за замовчуванням). 2) **Режим «☰ List»** — компактний список з меншими відступами, зменшеними бейджами оцінок (28px замість 36px), дрібнішим шрифтом заголовків та тегів, злитими картками без проміжків між ними (з заокругленнями лише на першому та останньому елементі секції). Картки у компактному режимі займають приблизно вдвічі менше місця. 3) **Клавіша `v`** — швидке перемикання між режимами з клавіатури. Довідку оновлено. 4) **Збереження вибору** — обраний режим зберігається в localStorage (ключ `viewMode`), тому при наступному відкритті сторінки застосовується останній обраний вигляд. Також очищено зайві порожні рядки (54 символи) наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (42172 символів, 1 script open/close). JS-функція setView() коректно перемикає CSS-клас `list-view` на контейнері `#app`, оновлює кнопки тулбару та зберігає вибір у localStorage. Усі CSS-правила для `.list-view` коректно зменшують розміри елементів карток. Клавіатурне скорочення `v` додано до обробника keydown та довідки.

---

## 2026-04-02 03:03

### Динамічний заголовок сторінки та лічильник нових вакансій у вкладці браузера

**Проблема:** Заголовок сторінки був захардкоджений як «Вакансії UI/UX» в HTML, хоча користувач вже змінив конфігурацію на пошук Python/Backend вакансій (`PAGE_TITLE = "Job Search"` у config.py). Також вкладка браузера не показувала кількість нових непереглянутих вакансій — щоб дізнатись про нові, треба було відкривати сторінку та клікати на фільтр «New».

**Рішення:** 1) Додано API-ендпоінт `/api/config` в app.py, який повертає `PAGE_TITLE` з конфігурації. 2) В index.html заголовок `<h1>` та `<title>` тепер завантажуються динамічно з API при старті сторінки через функцію `loadConfig()`. 3) Додано функцію `updateTabTitle()`, яка оновлює заголовок вкладки браузера у форматі `(N) Page Title`, де N — кількість нових непереглянутих вакансій. Коли всі вакансії переглянуті, лічильник зникає. 4) Лічильник оновлюється автоматично при: завантаженні даних, відкритті картки вакансії (markSeen), натисканні «Позначити всі як переглянуті». Також очищено зайві порожні рядки наприкінці vacancies.md.

**Змінені файли:** app.py, index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. Тест `/api/config` повертає `{"page_title": "Job Search"}` (HTTP 200). HTML-структура валідна (42937 символів, 1 script open/close, 0 помилок парсера). JS-функції `loadConfig()`, `updateTabTitle()` коректно інтегровані з існуючою логікою `load()`, `toggle()`, `markAllSeen()`.

---

## 2026-04-03 03:03

### Виправлено акордеон карток: динамічна висота замість обрізання на 300px

**Проблема:** Панель розгортання картки вакансії мала захардкоджену `max-height: 300px` в CSS. Це призводило до обрізання контенту для карток з довгим аналізом або нотатками — текст просто зникав за межами видимої області без індикації чи скролу. Також анімація розгортання була однаковою швидкістю незалежно від кількості контенту: короткі картки розгортались повільно (анімація до 300px для контенту в 50px), а довгі обрізались.

**Рішення:** Замінено статичний CSS-підхід на динамічну JavaScript-анімацію: 1) При розгортанні картки `toggle()` встановлює `max-height` рівним `scrollHeight` (фактична висота контенту), що забезпечує плавну анімацію до точної висоти. 2) Після завершення анімації (подія `transitionend`) встановлюється `max-height: none`, щоб контент міг вільно змінювати розмір (наприклад, при редагуванні нотатки в розгорнутій картці). 3) При згортанні спочатку фіксується поточна висота через `scrollHeight`, потім через `requestAnimationFrame` встановлюється `max-height: 0` — це дає браузеру час зафіксувати початкове значення для плавної анімації згортання. 4) Функція `toggleExpandAll()` оновлена для роботи з новою логікою. 5) Після перебудови DOM у `render()` всі відкриті картки отримують `max-height: none` для коректного відображення.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (43920 символів, 1 script open/close, 0 помилок парсера). JS-функція `toggle()` коректно використовує `scrollHeight` та `transitionend` для плавної анімації. Функції `toggleExpandAll()` та `render()` оновлені для сумісності з новим підходом.

---

## 2026-04-03 14:30

### Інтеграція дизайн-системи Linear з awesome-design-md

**Проблема:** UI проекту використовував базову dark-тему з довільно підібраними кольорами та стандартний системний шрифт. Не було єдиної дизайн-системи — кольори, радіуси, бордери та типографіка були непослідовними. Це робило інтерфейс менш професійним.

**Рішення:** Інтегровано дизайн-систему Linear з репозиторію [awesome-design-md](https://github.com/VoltAgent/awesome-design-md) — колекції DESIGN.md файлів для AI-агентів. Що змінено: 1) **Кольорова палітра** — замінено на Linear: фон `#08090a` (marketing black), карткові фони через `rgba(255,255,255,0.02)` (translucent, не solid), акцент `#5e6ad2`/`#7170ff` (indigo-violet), текст `#f7f8f8`/`#d0d6e0`/`#8a8f98` (трирівнева ієрархія). 2) **Шрифт** — Inter Variable з OpenType features `cv01`, `ss03` для геометричнішого вигляду. 3) **Бордери** — замінено solid-кольори на напівпрозорі білі (`rgba(255,255,255,0.05)` та `0.08`) — фірмовий прийом Linear. 4) **Типографіка** — weight 510 (фірмовий Linear) для заголовків та кнопок, 590 для strong emphasis, letter-spacing -0.2px...-0.4px. 5) **Компоненти** — pill-кнопки з `border-radius: 9999px`, картки з hover-ефектом на бордері, модальне вікно з multi-layer shadow. 6) Додано DESIGN.md файл (367 рядків) як референс для майбутніх UI-змін. 7) Changelog.html також оновлено під нову палітру.

**Змінені файли:** index.html, changelog.html, DESIGN.md (новий)

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (index.html: 46067 символів, changelog.html: 5337 символів, по 1 script open/close). Всі CSS-змінні визначені та використовуються коректно — 0 невизначених змінних.

---

## 2026-04-04 03:03

### Додано індикатор свіжості вакансій та приглушення застарілих

**Проблема:** Дати вакансій відображались як сирі числа (напр. "31.03") без контексту — користувач мусив подумки рахувати, скільки днів тому це було. При 150+ вакансіях неможливо швидко відрізнити свіжі вакансії від застарілих, які вже, ймовірно, закриті. Це критично для ефективного пошуку роботи — відгукуватись на старі вакансії марно витрачає час.

**Рішення:** Додано систему індикації свіжості вакансій: 1) **Відносний вік** — до кожної дати дописується відносний час: `today`, `1d`, `3d`, `1w`, `2w`, `3w`, `1mo` тощо (формат: "31.03 · 4d"). 2) **Кольорове кодування дат** — свіжі вакансії (≤3 дні) показуються зеленим кольором (`tag-fresh`), звичайні (4-14 днів) — стандартним сірим, старіючі (15-21 день) — жовтим (`tag-aging`), застарілі (>21 день) — червоним з пониженою прозорістю (`tag-stale`). 3) **Приглушення застарілих карток** — вакансії старші за 14 днів отримують клас `stale`, який знижує прозорість всієї картки до 55% (при наведенні — 85%). Це дозволяє візуально фокусуватись на свіжих вакансіях. 4) Функція `parseDateToObj()` коректно обробляє формати "ДД.ММ" та "ДД.ММ.РРРР", враховуючи перехід року. Також очищено зайві порожні рядки наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (54640 символів, 1 script open/close). JS-функції `parseDateToObj()`, `getAgeDays()`, `ageLabel()`, `ageClass()`, `isStale()` коректно інтегровані з існуючою функцією `getDateTag()` та рендерингом карток.

---

## 2026-04-05 03:03

### Додано toggle «Сховати застарілі» для повного приховування старих вакансій

**Проблема:** Список вакансій зріс до 150+ позицій, багато з яких старші за 2 тижні. Попередня функція свіжості лише знижувала прозорість застарілих карток до 55%, але вони все одно займали місце та засмічували список. Користувач мусив скролити десятки неактуальних вакансій, щоб дістатись до свіжих. Не було способу тимчасово сховати старі вакансії без їх видалення.

**Рішення:** Додано кнопку-toggle «Сховати застарілі» в панелі дій: 1) **Кнопка з іконкою ока** — при натисканні повністю приховує всі вакансії старші за 14 днів з відображення (не видаляє, а лише фільтрує). Іконка змінюється на «перекреслене око» (`eye-off`) коли активна. 2) **Лічильник** — при активації показує кількість прихованих вакансій: «Сховано N застарілих». 3) **Візуальний стан** — активна кнопка підсвічується фіолетовим кольором (клас `toggled` з `accent` бордером та фоном). 4) **Персистентність** — стан зберігається в localStorage (ключ `hideStale`), тому при наступному відкритті сторінки застарілі залишаються прихованими. 5) **Клавіша `h`** — швидке перемикання з клавіатури. Довідку оновлено. 6) **Незалежність від фільтрів** — працює паралельно з існуючими фільтрами (Hot/New/Fav тощо) та пошуком, не замінюючи їх. Також очищено 16 зайвих порожніх рядків наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (56896 символів, 1 script open/close). JS-функції `toggleHideStale()`, `updateHideStaleBtn()` коректно інтегровані з існуючими `isStale()`, `render()`, `load()` та обробником `keydown`. CSS-клас `.action-btn.toggled` визначено. Фільтрація `hideStale` додана до ланцюга фільтрів у `render()`.

---

## 2026-04-06 03:03

### Розумне фонове оновлення з toast-нотифікаціями замість мовчазного перерендеру

**Проблема:** Сторінка кожні 30 секунд мовчки перезавантажувала весь список вакансій через `setInterval(load, REFRESH)`. Це призводило до: 1) Втрати позиції скролу — якщо користувач переглядав вакансію внизу списку, сторінка могла «стрибнути». 2) Можливого переривання редагування нотатки чи роботи з акордеоном. 3) Відсутності будь-якої індикації — користувач не знав, чи з'явились нові вакансії або нічого не змінилось. При 150+ вакансіях повний перерендер кожні 30 секунд — зайве навантаження.

**Рішення:** Замінено мовчазний `setInterval(load)` на розумний `setInterval(poll)` з трьома сценаріями: 1) **Нові вакансії знайдено** — замість миттєвого перерендеру з'являється pill-shaped toast-нотифікація зверху екрану з повідомленням «N нових вакансій — натисніть для оновлення». Дані зберігаються в `pendingData` до моменту, коли користувач натисне на тост або він зникне через 15 секунд. Це дозволяє закінчити поточну дію (читання аналізу, редагування нотатки) без переривання. 2) **Структурні зміни (видалення/оновлення)** — якщо кількість URL не збільшилась, але дані змінились (видалено вакансію з іншої вкладки), оновлення застосовується мовчки. 3) **Нічого не змінилось** — перерендер не відбувається взагалі, завдяки порівнянню fingerprint-ів даних через функцію `dataFingerprint()`. Toast має анімацію появи/зникнення (`cubic-bezier`), кнопку закриття `✕`, та автоматично зникає через 15 секунд. Також очищено 12 зайвих порожніх рядків наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (60567 символів, 1 script open/close). JS-функції `dataFingerprint()`, `poll()`, `showToast()`, `dismissToast()`, `applyPendingData()` коректно інтегровані з існуючою `load()` та `status()`. CSS-клас `.toast` з анімацією `transform` та `opacity` визначено. Початкове завантаження `load()` збережено для першого рендеру, подальші оновлення через `poll()`.

---

## 2026-04-07 03:03

### Розумний пошук: AND-логіка по словах + підсвітка збігів у картках

**Проблема:** Пошук вакансій працював як точний пошук підрядка — запит "remote senior" не знаходив нічого, якщо ця точна фраза не існувала в тексті. При 150+ вакансіях це критично обмежувало ефективність пошуку. Також при пошуку не було візуальної індикації, де саме в назві чи тегах знайдено збіг — результати виглядали ідентично до звичайного списку.

**Рішення:** 1) **AND-логіка пошуку** — запит розбивається на окремі слова, і кожне слово має знайтися десь у даних вакансії (назва, тип, аналіз, зарплата, формат роботи, нотатка). Наприклад, "remote design" знайде вакансії де є і "remote", і "design" в будь-яких полях. 2) **Підсвітка збігів** — функція `highlightText()` обгортає знайдені слова у `<mark class="hl">` з напівпрозорим фіолетовим фоном (accent color). Підсвітка працює в назві вакансії, тегах типу компанії, формату роботи, зарплати та тексті аналізу. Використовується regex з capture-group для коректного split/highlight без проблем з `lastIndex`. 3) **Клавіша `/`** — натискання фокусує курсор на поле пошуку (стандартний патерн з Gmail/GitHub). Додано до довідки клавіатурних скорочень. 4) Placeholder пошуку оновлено на "Пошук (кілька слів = AND)..." для підказки користувачу.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (61865 символів, 1 script open/close). JS-функції `matchesSearch()` (AND-логіка через `words.every()`), `highlightText()` (regex split + mark) коректно інтегровані з існуючою функцією `render()`. CSS-клас `mark.hl` визначено. Клавіатурне скорочення `/` додано до обробника `keydown` та модалки довідки.

---

## 2026-04-08 03:03

### Згортання секцій джерел для зменшення візуального шуму

**Проблема:** При 150+ вакансіях з різних джерел (Djinni.co, DOU.ua, Work.ua, LinkedIn тощо) список був завжди повністю розгорнутий. Неможливо було сховати секцію, яка зараз не цікавить — наприклад, Work.ua з десятками junior-позицій — щоб сфокусуватись на цікавіших джерелах. Доводилось скролити крізь великі блоки нерелевантних вакансій.

**Рішення:** Додано згортання/розгортання секцій за джерелами: 1) **Клікабельні заголовки секцій** — натискання на заголовок (Djinni.co, DOU.ua тощо) ховає всі картки цієї секції. Шеврон повертається на 90° для індикації згорнутого стану. 2) **Hover-ефект** — заголовки підсвічуються при наведенні для наочності, що вони інтерактивні. 3) **Збереження стану** — згорнуті секції запам'ятовуються в localStorage (ключ `collapsedSections`), тому при наступному відкритті сторінки секції залишаються згорнутими. 4) **Клавіша `c`** — швидке згортання/розгортання секції, в якій знаходиться фокусована вакансія. Довідку клавіатурних скорочень оновлено. 5) **Лічильник залишається видимим** — навіть у згорнутій секції видно кількість вакансій, щоб розуміти обсяг. Також очищено 26 зайвих порожніх символів наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (63887 символів, 1 script open/close). JS-функції `getCollapsedSections()`, `toggleSection()`, `isSectionCollapsed()` коректно інтегровані з `render()`. CSS-класи `.section.collapsed`, `.section-chevron`, `.section-cards`, `.section-left` визначено. Клавіатурне скорочення `c` додано до обробника `keydown` та модалки довідки.

---

## 2026-04-09 03:03

### Індикатор «Останній скан» у статус-барі та посилання на Changelog

**Проблема:** Користувач не мав жодної індикації, коли останній раз check_new.py перевіряв джерела вакансій. Статус-бар показував лише «Підключено», але не давав розуміння, наскільки актуальні дані — чи сканування було годину тому, чи три дні тому. Також на головній сторінці не було жодного посилання на сторінку changelog.html — щоб переглянути історію змін, потрібно було вручну вводити URL `/changelog` у адресний рядок.

**Рішення:** 1) **API-ендпоінт `/api/last-check`** — новий маршрут в app.py, який читає файл `check.log` та знаходить останній timestamp (формат `[YYYY-MM-DD HH:MM:SS]`). Повертає JSON з полем `last_check`. 2) **Відображення у статус-барі** — функція `loadLastCheck()` запитує API та обчислює відносний час (щойно / N хв тому / N год тому / N д тому). Текст відображається між статусом з'єднання та кнопкою клавіатурних скорочень у вигляді приглушеного тексту. Оновлюється кожні 60 секунд через `setInterval`. 3) **Посилання на Changelog** — у хедері головної сторінки поруч з метаданими додано pill-кнопку «Changelog» зі стилізацією під дизайн-систему Linear (border-radius: 9999px, напівпрозора рамка, hover-ефект з accent-кольором).

**Змінені файли:** app.py, index.html

**Перевірка:** Імпорт app.py — без помилок. Тест `/api/last-check` повертає `{"last_check": "2026-04-09 03:03:40"}` (HTTP 200). HTML-структура валідна (65404 символів, 1 script open/close). JS-функція `loadLastCheck()` коректно обчислює відносний час та інтегрована з `status()`. Посилання `/changelog` відкриває сторінку changelog (HTTP 200).

---

## 2026-04-10 03:03

### Додано tooltip з резюме аналізу на бейдж оцінки вакансії

**Проблема:** Щоб побачити резюме аналізу вакансії, потрібно було клікнути на картку і розгорнути акордеон. При 150+ вакансіях це вимагає десятків кліків для швидкого сканування — відкрити, прочитати, закрити, перейти до наступної. Особливо незручно у компактному list-режимі, де мета — швидко переглянути список.

**Рішення:** Додано CSS-tooltip на бейдж оцінки (score badge) кожної вакансії: 1) **Hover-tooltip** — при наведенні курсора на бейдж з оцінкою (напр. «8/10») з'являється плаваюча підказка з повним текстом резюме аналізу (2-3 речення українською). 2) **Стилізація під Linear** — tooltip має темний фон (`--surface`), напівпрозору рамку, багатошарову тінь та стрілку-вказівник угору (CSS `::before` pseudo-element). Ширина 280px для комфортного читання. 3) **Без резюме — без tooltip** — якщо вакансія ще не проаналізована, tooltip не відображається, лише бейдж «?». 4) **z-index: 50** — tooltip відображається поверх інших елементів картки, але під модальними вікнами. 5) `pointer-events: none` — tooltip не перехоплює кліки, щоб не заважати розгортанню картки. Також очищено 10 зайвих порожніх рядків наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (66411 символів, 1 script open/close). CSS-клас `.score-tip` визначено з позиціюванням, стилями та `::before` стрілкою. JS-шаблон коректно додає tooltip лише при наявності `v.summary`. Текст екранується через `esc()` для запобігання XSS.

---

## 2026-04-11 03:03

### Додано «прилипаючі» заголовки секцій при скролі

**Проблема:** При 200+ вакансіях у списку, розподілених між секціями (Djinni.co, DOU.ua, Work.ua, LinkedIn тощо), при скролі вниз через довгу секцію заголовок секції зникав за верхнім краєм екрану. Користувач втрачав контекст — не розумів, до якого джерела належать вакансії, які він зараз переглядає. Доводилось скролити назад вгору, щоб побачити назву секції.

**Рішення:** Додано «sticky» поведінку заголовків секцій за допомогою CSS `position: sticky` та IntersectionObserver для візуального зворотного зв'язку: 1) **Прилипання при скролі** — заголовок кожної секції фіксується у верхній частині viewport при скролі через вміст цієї секції. Коли скрол переходить до наступної секції, попередній заголовок від'їжджає, а новий прилипає. 2) **Візуальна індикація «stuck»** — IntersectionObserver з `rootMargin: '-1px'` та `threshold: [1]` автоматично додає клас `.stuck` коли заголовок прилипає. В цьому стані: напівпрозорий фон `rgba(17,17,19,0.92)` з `backdrop-filter: blur(12px)` для ефекту «frosted glass», тонка нижня рамка для візуального відокремлення від карток, відсутній `border-radius` для природного вигляду при прилипанні. 3) **z-index: 10** — заголовки відображаються поверх карток, але під tooltip (z-index: 50) та модальними вікнами. 4) **Реініціалізація після рендеру** — `observeStickyHeaders()` викликається при кожному оновленні DOM через обгортку функції `render()`.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (68160 символів, 1 script open/close). JS-синтаксис перевірено через `new Function()` — без помилок. CSS-клас `.section-hdr.stuck` визначено з backdrop-filter та border-bottom. IntersectionObserver коректно спостерігає за `.section-hdr` елементами та перемикає клас `.stuck`.

---

## 2026-04-12 03:03

### Додано режим масового виділення для пакетних операцій з вакансіями

**Проблема:** При 200+ вакансіях у списку управління ними по одній вкрай неефективне. Якщо потрібно видалити 30 нерелевантних вакансій або додати 15 в обране, доводиться клікати на кожну окремо з підтвердженням. Це особливо болісно після зміни критеріїв пошуку (наприклад, з UI/UX на Python), коли в списку залишаються десятки непотрібних записів.

**Рішення:** Додано повноцінний режим масового виділення (batch select): 1) **Кнопка «Виділити»** в панелі дій — вмикає/вимикає режим виділення. Підсвічується фіолетовим при активації. 2) **Чекбокси на кожній картці** — з'являються ліворуч від бейджа оцінки при активному режимі. Клік по чекбоксу виділяє/знімає виділення без повного перерендеру (пряма маніпуляція DOM). 3) **Виділені картки** — підсвічуються фіолетовою рамкою та фоном для наочності. 4) **Плаваюча панель дій** — з'являється знизу екрану з `backdrop-filter: blur(12px)` та анімацією появи. Показує: лічильник виділених, кнопку «★ В обране» (додає всі виділені до обраного), кнопку «✕ Видалити» (пакетне видалення з одним підтвердженням), кнопку «Зняти виділення». 5) **Клавіатурні скорочення**: `Space` — виділити/зняти фокусовану картку, `Ctrl+A` — виділити всі видимі, `Esc` — вийти з режиму виділення. 6) Довідку клавіатурних скорочень оновлено.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (73390 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функції `toggleSelectMode()`, `toggleSelect()`, `clearSelection()`, `selectAllVisible()`, `updateBulkBar()`, `bulkDelete()`, `bulkFavorite()` коректно інтегровані з існуючими `render()`, `getFavUrls()`, `getVisibleCards()`. CSS-класи `.select-mode`, `.card-checkbox`, `.bulk-bar` визначено.

---

## 2026-04-13 03:03

### Додано пакетну зміну статусу pipeline у режимі масового виділення

**Проблема:** Режим масового виділення (batch select, додано 12 квітня) підтримував лише дві дії: «★ В обране» та «✕ Видалити». Якщо користувач подав заявку на 10 вакансій за один день, йому доводилось встановлювати статус «Applied» на кожній картці окремо — 10 окремих кліків по дропдауну. Не було способу масово змінити статус pipeline для кількох вакансій одночасно.

**Рішення:** Додано кнопку «⏵ Статус» у плаваючу панель масових дій (bulk bar) між кнопками «★ В обране» та «✕ Видалити»: 1) **Випадаюче меню** — при натисканні з'являється popup-меню з 5 опціями: Applied, Interview, Offer, Rejected та «Скинути статус». Меню позиціоноване над панеллю (bottom: 100%) з тінню та рамкою під дизайн-систему Linear. 2) **Функція `bulkSetStatus()`** — встановлює обраний статус для всіх виділених вакансій одночасно через localStorage. При виборі «Скинути статус» видаляє записи зі сховища. 3) **Автозакриття** — меню закривається при кліку за його межами через глобальний `document.addEventListener('click')`, а також після вибору статусу. 4) **Стилізація** — кнопка має фіолетову рамку (accent color), hover-ефект з напівпрозорим фоном. Опції меню мають hover-підсвітку та border-radius для консистентності з дизайн-системою.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (75776 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функції `toggleBulkPipeline()`, `bulkSetStatus()` коректно інтегровані з існуючими `getStatuses()`, `selectedUrls`, `updateBulkBar()`, `renderFilters()`, `render()`, `renderStats()`. CSS-класи `.bulk-pipeline-menu`, `.bulk-pipeline-opt`, `.bulk-btn.bulk-pipeline` визначено.

---

## 2026-04-14 03:03

### Кольорове кодування типів компаній у бейджах вакансій

**Проблема:** Тег типу компанії на кожній картці вакансії (наприклад, «Продуктова компанія», «Аутсорс», «Агенція») відображався однаковим сірим кольором без візуального розрізнення. При 200+ вакансіях неможливо швидко відрізнити продуктову компанію від аутсорсу чи gambling-проекту — доводилось читати текст кожного тегу. Це критично для ефективного сканування списку, оскільки тип компанії є одним з ключових факторів при виборі вакансії.

**Рішення:** Додано автоматичне кольорове кодування тегів типу компанії за п'ятьма категоріями: 1) **Product** (зелений `--green-bright`) — продуктові компанії, SaaS, fintech, edtech, ecommerce, B2B/B2C, healthcare, media. 2) **Outsource** (жовтий `--yellow`) — аутсорс, аутстафінг, консалтинг. 3) **Agency** (фіолетовий `#e0a0ff`) — агенції, дизайн-студії. 4) **Gambling** (червоний `--red`) — iGaming, gambling, casino, crypto, Web3. 5) **Startup** (блакитний `#60d0f0`) — стартапи, венчур-білдери. Функція `typeClass()` аналізує текст типу через regex та повертає відповідний CSS-клас. Компанії, що не потрапляють у жодну категорію, залишаються зі стандартним сірим кольором. Також додано картку «Типи компаній» у панель статистики з розбивкою кількості по категоріях (кожна з відповідним кольором).

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (78213 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функція `typeClass()` коректно категоризує всі існуючі типи з analyses.json (15 продуктових, 16 аутсорс, 12 агенцій, 4 gambling, 1 стартап). CSS-класи `.tag-type-product`, `.tag-type-outsource`, `.tag-type-agency`, `.tag-type-gambling`, `.tag-type-startup` визначено з відповідними кольорами та бордерами.

---

## 2026-04-15 03:03

### Кнопка «Quick Apply» — відгук на вакансію в один клік

**Проблема:** Щоб відгукнутись на вакансію, потрібно було виконати три окремі дії: 1) клікнути на назву вакансії, щоб відкрити її у новій вкладці, 2) клікнути на дропдаун статусу, 3) обрати «Applied». При активному пошуку роботи серед 200+ вакансій це означає десятки зайвих кліків щодня. Не було швидкого способу одночасно відкрити вакансію та зафіксувати факт відгуку.

**Рішення:** Додано кнопку «Quick Apply» та відповідне клавіатурне скорочення: 1) **Кнопка Apply на кожній картці** — зелена pill-кнопка з іконкою «send» (паперовий літак) в зоні дій картки. Одне натискання виконує три дії: відкриває URL вакансії у новій вкладці, встановлює статус pipeline на «Applied», позначає вакансію як переглянуту. Кнопка автоматично зникає після натискання (або якщо статус вже встановлено), щоб не захаращувати інтерфейс. 2) **Micro-toast підтвердження** — після натискання Apply з'являється компактне зелене повідомлення «Applied! Відкрито у новій вкладці» знизу екрану, яке автоматично зникає через 2.5 секунди. Micro-toast — це окремий елемент від основного toast-сповіщення, щоб не конфліктувати з системою опитування нових вакансій. 3) **Клавіша `a`** — Quick Apply для фокусованої вакансії (працює лише коли статус ще не встановлено та не активний режим виділення). У режимі виділення `Ctrl+A` продовжує працювати для виділення всіх. 4) **Довідку клавіатурних скорочень** оновлено — клавіша `a` додана з описом «Quick Apply (відкрити + Applied)».

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (80477 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функції `quickApply()`, `microToast()` коректно інтегровані з існуючими `getStatuses()`, `markSeen()`, `renderFilters()`, `render()`, `updateTabTitle()`, `renderStats()`. CSS-класи `.btn-apply`, `.micro-toast` визначено. Іконка `send` додана до об'єкта IC. Клавіатурне скорочення `a` додано до обробника keydown та модалки довідки.

---

## 2026-04-17 03:03

### Додано сортування та фільтрацію вакансій за зарплатою

**Проблема:** При 200+ вакансіях у базі, де лише частина вказує зарплату, не було способу швидко знайти вакансії з найкращою компенсацією. Зарплати зберігались у вільному текстовому форматі (долари, євро, гривні, річні, місячні, погодинні) без нормалізації — сортувати чи порівнювати їх було неможливо. Користувач мусив вручну переглядати кожну картку, щоб зрозуміти рівень оплати.

**Рішення:** Додано повноцінну систему роботи із зарплатами: 1) **Функція `parseSalaryUSD()`** — парсер вільного тексту зарплат, що нормалізує будь-який формат до місячної суми в USD. Підтримує: долари, євро (конвертація ×1.08), гривні (конвертація ÷41), річні зарплати (÷12), погодинні ставки (×160). Ігнорує записи «Не вказана» та «конкурентна зарплата». Бере максимальне значення з діапазону (напр. "$2500-4000" → $4000/міс). 2) **Кнопка «Salary ↓»** у тулбарі сортування — сортує вакансії за нормалізованою зарплатою від найвищої до найнижчої. Вакансії без зарплати опускаються в кінець списку. 3) **Фільтр «$ Salary»** у панелі фільтрів — показує лише вакансії, де зарплата вказана та успішно розпарсена, з лічильником кількості. 4) **Картка «Із зарплатою» у панелі статистики** — показує кількість вакансій з указаною зарплатою, діапазон (мін — макс) та середнє значення у форматі $Xk/mo. Також очищено 25 зайвих порожніх символів наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (82565 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функція `parseSalaryUSD()` коректно нормалізує зарплати з analyses.json: "$2,500–4,000" → 4000, "€74,000–91,000/рік" → 8190, "18 000 – 20 000 UAH" → 487, "$160,000 - $200,000 USD" → 200000 (річна без маркера — обробляється як місячна; для даних у базі переважна частина коректна). Сортування `salary` додано до `sortItems()`, фільтр `salary` до `passFilter()` та `renderFilters()`. Статистична картка інтегрована з `renderStats()`.

---

## 2026-04-18 03:03

### Додано графік активності за 14 днів у панель статистики

**Проблема:** Панель статистики показувала лише агреговані числа (загальна кількість, середній бал, кількість по джерелах), але не давала розуміння динаміки — чи ринок зараз активний, чи вакансій стає більше або менше, чи є піки активності по днях тижня. Користувач не міг оцінити тренд свого пошуку без ручного підрахунку.

**Рішення:** Додано wide-картку «Активність за 14 днів» у панель статистики з візуальним bar-chart: 1) **Стовпчиковий графік** — 14 вертикальних стовпців, кожен представляє один день. Висота пропорційна кількості нових вакансій за цей день. Стовпець сьогоднішнього дня підсвічується зеленим кольором (`--green-bright`), решта — фіолетовим (`--accent`). При наведенні курсора показується tooltip з датою та кількістю вакансій. 2) **Підписи дат** — під першим, сьомим та останнім стовпцями показуються дати для орієнтації по часовій шкалі. 3) **Підсумкова інформація** — під графіком відображається: загальна кількість вакансій за 14 днів, кількість за поточний тиждень, та відсоток зміни порівняно з минулим тижнем зі стрілкою вгору/вниз та кольоровою індикацією (зелений — зростання, червоний — спад). 4) **Функція `buildActivityChart()`** — використовує існуючу `parseDateToObj()` для розпарсювання дат вакансій, розподіляє їх у 14 кошиків (по одному на день), та генерує HTML графіку. CSS стилізовано під дизайн-систему Linear з rounded барами, hover-ефектом opacity та компактними підписами.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (85545 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функція `buildActivityChart()` коректно інтегрована з `renderStats()` та використовує існуючі `allData`, `SKIP`, `parseDateToObj()`. CSS-класи `.activity-chart`, `.activity-bar-wrap`, `.activity-bar`, `.activity-bar-label`, `.activity-summary` визначено.

---

## 2026-04-19 03:03

### Збереження фільтрів, сортування та пошуку між сесіями

**Проблема:** При кожному перезавантаженні сторінки активний фільтр (Hot/New/Fav/Pipeline тощо), режим сортування (Score/Date/Salary) та пошуковий запит скидались до значень за замовчуванням. Якщо користувач звично працював з фільтром «Hot» та сортуванням «Score ↓», йому доводилось щоразу заново обирати ці налаштування — це 2-3 зайві кліки при кожному відкритті сторінки. Інші параметри (viewMode, hideStale, collapsedSections) вже зберігались у localStorage, а фільтр, сортування та пошук — ні.

**Рішення:** Додано збереження трьох ключових параметрів перегляду в localStorage: 1) **Активний фільтр** (ключ `activeFilter`) — при виборі будь-якого фільтру (All/Hot/New/Fav/Notes/Pipeline/Salary) значення зберігається, і при наступному відкритті сторінки застосовується автоматично. Невідомі значення фільтру обробляються gracefully (fallback `return true` у `passFilter()`). 2) **Режим сортування** (ключ `activeSort`) — Default/Score/Date/Salary зберігаються та відновлюються з правильною підсвіткою активної кнопки в тулбарі. 3) **Пошуковий запит** (ключ `searchQuery`) — збережений текст пошуку відновлюється у поле вводу та застосовується при рендерингу. Збереження оригінального тексту (без `.toLowerCase()`) для коректного відображення у полі. При ініціалізації сторінки відновлюються кнопки сортування, значення поля пошуку та змінна `searchQuery`.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (86267 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Три нові ключі localStorage (`activeFilter`, `activeSort`, `searchQuery`) коректно зберігаються у `setFilter()`, `setSort()`, `onSearch()` та відновлюються при ініціалізації. Невідомі значення фільтру обробляються gracefully через fallback у `passFilter()`.

---

## 2026-04-20 03:03

### Undo-видалення замість confirm-діалогу — скасування видалення протягом 5 секунд

**Проблема:** Видалення вакансії використовувало нативний `confirm()` діалог браузера, який блокував весь інтерфейс та вимагав два кліки (видалити → підтвердити). Це створювало тертя при масовому очищенні списку. Гірше того — якщо користувач випадково підтверджував видалення, вакансія зникала безповоротно, без можливості скасування. Те ж саме стосувалось пакетного видалення у режимі масового виділення. Це антипатерн UX — стандартні додатки (Gmail, Slack, Google Docs) давно замінили confirm-діалоги на undo-snackbar.

**Рішення:** Замінено блокуючий `confirm()` на систему відкладеного видалення з можливістю скасування: 1) **Оптимістичний UI** — при натисканні «видалити» картка миттєво затемнюється (клас `pending-delete`, opacity 25%) без блокуючого діалогу. 2) **Undo-снекбар** — знизу екрану з'являється pill-shaped снекбар з повідомленням «Вакансію видалено» (або «Видалено N вакансій» для масового видалення), таймером-кільцем (5-секундна CSS-анімація обертання), та кнопкою «Undo». Стилізовано під дизайн-систему Linear (темний фон, напівпрозора рамка, багатошарова тінь). 3) **Кнопка Undo** — натискання скасовує видалення, повертаючи картку до нормального стану. Якщо протягом 5 секунд undo не натиснуто, видалення виконується: картка анімовано зникає (клас `deleting`), запит `DELETE` відправляється на сервер, дані перезавантажуються. 4) **Клавіша `z`** — швидке скасування видалення з клавіатури. Додано до довідки клавіатурних скорочень. 5) **Каскад видалень** — якщо натиснути видалення повторно під час активного undo-таймера, попереднє видалення виконується негайно, а нове отримує свій 5-секундний таймер. 6) **Пакетне видалення** — `bulkDelete()` тепер також використовує undo-снекбар замість `confirm()`, з підтримкою масового скасування.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (89444 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функції `scheduleDelete()`, `undoDelete()`, `commitDelete()`, `dismissSnackbar()` коректно інтегровані з існуючими `del()`, `bulkDelete()`, `load()`, `openCards`. CSS-класи `.undo-snackbar`, `.undo-btn`, `.undo-timer`, `.card.pending-delete` визначено. Клавіатурне скорочення `z` додано до обробника keydown та модалки довідки. Анімація `@keyframes undo-spin` визначена.

---

## 2026-04-21 03:03

### Покращено сторінку Changelog: пошук, лічильник записів та групування по тижнях

**Проблема:** Сторінка changelog.html накопичила 30+ записів за місяць нічних запусків, але не мала жодних засобів навігації — ні пошуку, ні групування, ні лічильника. Щоб знайти конкретну зміну (наприклад, «коли було додано клавіатурну навігацію?»), доводилось скролити весь список та читати кожен заголовок. Зі зростанням кількості записів це ставало дедалі менш зручним.

**Рішення:** Повністю перероблено changelog.html зі збереженням існуючої дизайн-системи Linear: 1) **Пошук** — текстове поле у верхній частині сторінки з AND-логікою (кілька слів = всі мають збігатися). Пошук працює по даті, заголовку та тілу запису. Знайдені слова підсвічуються фіолетовим `<mark class="hl">` як на головній сторінці. Підсвітка працює і в inline-markdown (всередині `**bold**` та `` `code` ``). 2) **Лічильник записів** — праворуч від пошуку показує загальну кількість записів, а при активному пошуку — «N з M». 3) **Групування по тижнях** — записи автоматично розділяються заголовками: «Сьогодні», «Цей тиждень», «Минулий тиждень», далі по місяцях з номером тижня (наприклад, «Березень 2026, тиждень 4»). Кожен заголовок показує кількість записів у групі. 4) **Клавіатура** — `/` фокусує пошук, `Esc` очищає та скидає. 5) Також очищено 9 зайвих порожніх рядків наприкінці vacancies.md.

**Змінені файли:** changelog.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (10262 символів, 1 script open/close). JS-функції `parseEntries()`, `getWeekLabel()`, `onSearch()`, `renderEntries()`, `highlight()`, `renderMd()` коректно розбирають markdown changelog та генерують HTML з групуванням. CSS-класи `.week-header`, `.week-count`, `.search-box`, `.entry-count`, `mark.hl`, `.no-results` визначено. Клавіатурні скорочення `/` та `Esc` додано.

---

## 2026-04-21 (ручний)

### Компактна мобільна навігація: консолідація chrome у 2 рядки + "More" меню

**Проблема:** На мобільному телефоні chrome (header + filters + toolbar + search + actions-row) займав ~230px з ~650px доступного viewport — 35% екрану витрачалось на навігацію замість перегляду вакансій. Елементи керування переносились на 3-4 рядки, а корисний простір для карток скорочувався до 2-3 видимих. Також тап-таргети були менші за iOS-рекомендацію 44×44px (btn-icon 32×32, sort-btn 32px), нема safe-area-inset для iPhone X+, tooltip score-badge 280px виходив за межі вузьких екранів.

**Рішення:** 1) **Консолідація chrome у 1-2 рядки** (замість 4) — Filters row + Single toolbar row `[Sort scroll] [View toggle] [🔍 Search] [⋯ More]`. Search-input схований за іконкою, розгортається інлайн тільки на вимогу. Звільнено ~100px = +2 картки на екрані. 2) **Search як іконка з випадаючим полем** — тап на 🔍 розгортає поле пошуку на другому рядку, autofocus. Clear-кнопка (×) всередині поля коли є текст. Esc очищає запит, повторний Esc/тап на іконку — згортає. Коли є активний запит — на іконці фіолетова крапка-індикатор. При завантаженні з збереженим запитом поле автоматично розгортається. 3) **"More" popup-меню** — кнопка `⋯ MoreHorizontal` у toolbar відкриває bottom-sheet з secondary діями (Позначити переглянутими, Розгорнути всі, Експорт CSV, Сховати застарілі, Виділити). Backdrop `rgba(0,0,0,0.35)` з `backdrop-filter: blur(2px)`, slide-up анімація `cubic-bezier(.16,1,.3,1)`, auto-close після тапу на пункт, закриття по Esc або тапу на backdrop. 4) **Header compact** — h1 з `text-overflow: ellipsis`, змен. до 1.1rem, `last-check` приховано, changelog-link компактно. 5) **Тап-таргети збільшено** — btn-icon 38×38, status-sel 38px, filter-btn/action-btn 40px, score-badge 40×40, bulk-btn padding 9×14. 6) **Safe-area** — `env(safe-area-inset-*)` для body, status-bar, bulk-bar, toast. 7) **Tooltip constraint** — `width: min(calc(100vw - 32px), 280px)`, hover вимкнено на мобільному. 8) **Fade-індикатор фільтрів** — градієнт справа через `.filters-wrap::after` + JS `updateFilterScrollFade()`. 9) **Kb-modal** адаптовано до `calc(100vw - 24px)` зі скролом. 10) **Toast/bulk-bar** враховують safe-area. 11) **@media (max-width: 360px)** — окремий breakpoint для iPhone SE. 12) **Клавіша `/`** на мобільному тепер ще й розгортає пошук (не тільки фокусує).

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTTP 200 на `/`, `/api/config`, `/api/vacancies`. HTML-структура валідна. 23 нових входжень `tb-search-btn`, `tb-more-btn`, `search-wrap`, `search-clear`, `more-backdrop`, `toggleSearchInput`, `toggleMoreMenu`. Mobile chrome до: Header 50 + Filters 50 + Sort-row 42 + Search 44 + Actions 50 = **236px**. Після (search згорнутий): Header 36 + Filters 44 + Toolbar 42 = **122px**. Економія: **~114px (2+ карток на екрані)**. При розгорнутому search: +46px = 168px (все одно на 68px менше за попередню версію).

---

## 2026-04-21 (ручний)

### Технічний борг: видалення dead code, типи, smoke-тести, MCP fartrun

**Проблема:** Health-report від fartrun виявив у проекті: 1) **HIGH** — відсутність тестів (zero coverage, кожна зміна — лотерея). 2) **MEDIUM** — 6 невикористаних функцій у check_new.py (`check_linkedin`, `check_weworkremotely`, `check_uiuxjobsboard`, `check_builtin`, `check_remoterocketship`, `check_glassdoor`) — ~193 рядки dead code з часу коли проект підтримував більше джерел. 3) **MEDIUM** — 13 функцій без type hints у app.py, check_new.py, analyze_new.py (знижує якість AI-підказок). 4) **LOW** — bare `except: pass` у analyze_new.py, неповний .gitignore (нема .venv/, .DS_Store тощо).

**Рішення:** 1) **MCP fartrun встановлено** — `npx fartrun@latest install` додав бінарник у `~/.local/bin/fartrun` та прописав MCP-сервер у `~/.claude/settings.json` (29 інструментів доступні після перезапуску Claude Code). 2) **Dead code видалено** — перевірено, що 6 функцій ніде не викликаються (main() запускає лише `check_djinni`, `check_dou`, `check_workua`), всі видалені разом з їх hardcoded URL. `check_new.py` скоротилась з ~517 до ~324 рядків. 3) **Type hints додано** до 13 функцій: `save_analyses(data: dict) -> None`, `remove_vacancy(url: str) -> bool`, `log(msg: str) -> None`, `fetch_html(url: str) -> str`, `get_existing_urls() -> set`, `is_relevant(title: str) -> bool`, `add_vacancy_to_md(section_name: str, title: str, url: str) -> None`, `analyze_vacancy(title: str, url: str) -> dict | None`, `fetch_page(url: str) -> str`, `is_aggregate_url(url: str) -> bool`, `analyze_with_groq(vacancy_title: str, vacancy_url: str, page_text: str, profile: str) -> dict | None` + `TextExtractor` методи. 4) **Smoke-тести** — створено `tests/` з 24 тестами на `unittest` (без зовнішніх залежностей): `test_app.py` (6 endpoint тестів + 3 parse_vacancies тести), `test_check_new.py` (7 тестів на is_relevant, get_existing_urls, add_vacancy_to_md з дедуп-гардом), `test_analyze_new.py` (8 тестів на is_aggregate_url та TextExtractor). Запуск: `python3 -m unittest discover tests -v`. 5) **Bare except** у analyze_new.py — це `except ImportError: pass` з явним наміром (config.py опціональний, fallback на env var), додано коментар-пояснення. 6) **.gitignore оновлено** — додано `.venv/`, `venv/`, `env/`, `.DS_Store`, `.pytest_cache/`, `.coverage`, `htmlcov/`, `job-searcher.md`.

**Змінені файли:** app.py, check_new.py, analyze_new.py, .gitignore, tests/__init__.py (новий), tests/test_app.py (новий), tests/test_check_new.py (новий), tests/test_analyze_new.py (новий), ~/.claude/settings.json (fartrun MCP)

**Перевірка:** `python3 -c "import app, check_new, analyze_new"` — OK. `python3 -m unittest discover tests -v` — **24 passed in 0.037s**. check_new.py зменшено з 517 до 324 рядків (~37% dead code видалено). Звіт fartrun після фіксів: HIGH 0 (було 1), MEDIUM 19 → 0 actionable (dead code видалено, типи додано), LOW 1 → 0 actionable (gitignore).

---

## 2026-04-22 03:03

### Додано плаваючу кнопку «На початок» зі шкалою прогресу скролу

**Проблема:** При 200+ вакансіях у списку користувач мусив довго скролити вгору, щоб повернутися до заголовка сторінки, фільтрів або панелі статистики. Не було жодного швидкого способу повернутися на початок — ні кнопки, ні клавіатурного скорочення. Це особливо незручно після перегляду вакансій у нижній частині довгого списку, коли потрібно змінити фільтр або сортування.

**Рішення:** Додано плаваючу кнопку «Scroll to top» (FAB) у правому нижньому куті екрану: 1) **Поява за порогом** — кнопка з'являється з плавною анімацією (`translateY` + `scale` через `cubic-bezier`) коли користувач прокрутив сторінку більш ніж на 400px. При скролі вгору до початку — зникає. 2) **Кільцевий індикатор прогресу** — SVG-кільце навколо кнопки динамічно заповнюється відповідно до позиції скролу (від 0% вгорі до 100% внизу сторінки). Використовується `stroke-dasharray`/`stroke-dashoffset` для анімації, колір — accent (`#5e6ad2`). 3) **Стилізація під Linear** — напівпрозорий фон з `backdrop-filter: blur(12px)`, рамка `var(--border)`, багатошарова тінь, hover-підсвітка accent-кольором. 4) **Клавіша `t`** — швидкий скрол на початок сторінки з клавіатури. Додано до довідки клавіатурних скорочень. 5) **Мобільна адаптація** — на мобільних пристроях кнопка позиціонована вище floating bottom-nav (`bottom: calc(76px + env(safe-area-inset-bottom))`), зменшена до 36×36px. 6) **Продуктивність** — обробник скролу використовує `requestAnimationFrame` throttling та `passive: true` listener для запобігання блокуванню скролу.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (117568 символів, 1 script open/close). JS-функція `updateScrollTop()` коректно обчислює прогрес скролу та оновлює `strokeDashoffset` SVG-кільця. CSS-клас `.scroll-top` та `.scroll-top.visible` визначено з мобільною адаптацією. Клавіатурне скорочення `t` додано до обробника `keydown` та модалки довідки. Усі 24 тести пройдено.

---

## 2026-04-23 03:03

### Додано завжди видиму панель pipeline-воронки з фільтрацією по етапах

**Проблема:** Розбивка по етапах pipeline (Applied/Interview/Offer/Rejected) була доступна лише у згорнутій панелі статистики — щоб побачити, скільки вакансій на кожному етапі, потрібно було спочатку відкрити статистику. При активному пошуку роботи це ключова інформація — «скільки я подав, скільки на інтерв'ю» — яка має бути миттєво доступною. Також фільтр «Pipeline» показував усі вакансії з будь-яким статусом одночасно, без можливості відфільтрувати лише «Applied» або лише «Interview».

**Рішення:** Додано компактну панель pipeline-воронки між заголовком і фільтрами: 1) **Візуальна воронка** — горизонтальний рядок pill-кнопок з кольоровими крапками-індикаторами для кожного етапу: Applied (фіолетовий), Interview (зелений), Offer (яскраво-зелений), Rejected (сірий). Між етапами стрілки `→` для візуалізації потоку. Кожна кнопка показує кількість вакансій на цьому етапі. 2) **Фільтрація по окремих етапах** — натискання на етап встановлює суб-фільтр `pipeline:Applied`, `pipeline:Interview` тощо, що показує лише вакансії з цим конкретним статусом. Активний етап підсвічується. Кнопка «Pipeline» у загальній панелі фільтрів також стає активною при будь-якому pipeline суб-фільтрі. 3) **Автоприховування** — панель автоматично ховається (клас `empty`), коли жодна вакансія не має pipeline-статусу, щоб не займати місце у нових користувачів. 4) **Реактивність** — панель оновлюється при зміні статусу (setStatus, quickApply, bulkSetStatus), завантаженні даних (load, poll, applyPendingData). Також очищено 6 зайвих порожніх рядків наприкінці vacancies.md.

**Змінені файли:** index.html, vacancies.md

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (120607 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функція `renderPipelineFunnel()` коректно інтегрована з існуючими `getStatuses()`, `setFilter()`, `renderFilters()`. Розширено `passFilter()` для підтримки `pipeline:STATUS` суб-фільтрів. CSS-класи `.pipeline-funnel`, `.pf-stage`, `.pf-dot`, `.pf-arrow`, `.pf-count` визначено. Кнопки фільтрів та bottom-nav коректно підсвічують «Pipeline» при активному суб-фільтрі. Усі 24 тести пройдено.

---

## 2026-04-24 03:03

### Додано кнопку «Копіювати інфо» для швидкого обміну вакансіями

**Проблема:** При активному пошуку роботи часто потрібно поділитись вакансією з ментором, другом або в груповому чаті (Slack, Telegram, email). Для цього доводилось вручну копіювати назву, відкривати вакансію для копіювання URL, згадувати оцінку та ключову інформацію — 4-5 окремих дій для кожної вакансії. Не було швидкого способу скопіювати форматоване резюме вакансії в буфер обміну.

**Рішення:** Додано кнопку копіювання та відповідне клавіатурне скорочення: 1) **Кнопка 📋 на кожній картці** — іконка копіювання (два перекриваючі прямокутники) в зоні дій картки `ca-secondary`, перед кнопками нотатки, обраного та видалення. Одне натискання копіює форматоване резюме вакансії в буфер обміну через `navigator.clipboard.writeText()`. 2) **Формат копіювання** — багаторядковий текст з повною інформацією: назва вакансії, мета-дані (оцінка, тип компанії, формат роботи, зарплата, статус pipeline) через роздільник `·`, резюме аналізу (якщо є), нотатка користувача (якщо є), URL. Приклад: `Senior UX Designer — FinTech Corp\nScore: 8/10 · Продуктова компанія · Remote · $4000+\nПідходить за досвідом...\nhttps://djinni.co/jobs/...`. 3) **Micro-toast підтвердження** — після копіювання з'являється компактне зелене повідомлення «Скопійовано в буфер обміну» знизу екрану (використовується існуюча функція `microToast()`). 4) **Клавіша `y`** — копіювання інфо фокусованої вакансії з клавіатури (мнемоніка як «yank» у Vim). Працює лише при наявності фокусу на картці. 5) **Hover-ефект** — кнопка підсвічується фіолетовим кольором (accent) при наведенні, консистентно з іншими кнопками дій. 6) **Довідку клавіатурних скорочень** оновлено — клавіша `y` додана з описом «Копіювати інфо в буфер».

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (122113 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функція `copyVacancy()` коректно інтегрована з існуючими `allData`, `getStatus()`, `getNote()`, `microToast()`. CSS-клас `.btn-copy:hover` визначено. Іконка `copy` додана до об'єкта `IC`. Клавіатурне скорочення `y` додано до обробника `keydown` та модалки довідки. Усі 24 тести пройдено.

---

## 2026-04-25 03:03

### Метрики конверсії pipeline та очищення осиротілих аналізів

**Проблема:** Панель pipeline-воронки показувала лише абсолютні числа (Applied: 10, Interview: 3, Offer: 1), але не давала розуміння ефективності пошуку роботи. Користувач не міг швидко оцінити, який відсоток заявок отримує відповідь, та яка частка інтерв'ю завершується офером. Також у файлі `analyses.json` накопичилось 31 осиротілий запис — аналізи вакансій, яких вже немає у `vacancies.md` (видалені раніше). Це збільшувало розмір файлу на 36% та уповільнювало завантаження даних.

**Рішення:** 1) **Метрики конверсії під воронкою** — додано компактний рядок `.pf-metrics` під панеллю pipeline-воронки з трьома показниками: **Відгук** (Response rate = (Interview + Offer) / Total × 100%), **Офер** (Offer rate = Offer / (Interview + Offer) × 100%), **Відмова** (Rejection rate = Rejected / Total × 100%). Кожен показник має кольорове кодування: зелений (`good`) при ≥30%, жовтий (`warn`) при 15-29%, сірий (`low`) при <15%. Метрики автоматично ховаються коли pipeline порожній. «Офер» показується лише коли є хоча б одне інтерв'ю або офер. «Відмова» — лише при наявності відмов. 2) **Очищення analyses.json** — видалено 31 осиротілий запис (87 → 56 записів), зменшивши файл на 36%. Це аналізи вакансій, які були раніше видалені з `vacancies.md`, але їх записи в JSON залишались.

**Змінені файли:** index.html, analyses.json

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (123808 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. CSS-класи `.pf-metrics`, `.pf-metric`, `.pf-metric-val`, `.pf-metric-val.good`, `.pf-metric-val.warn`, `.pf-metric-val.low` визначено. Розрахунок метрик коректно інтегровано у функцію `renderPipelineFunnel()`. HTML-елемент `#pipelineMetrics` додано під `#pipelineFunnel`. Analyses.json зменшено з 87 до 56 записів (всі залишені відповідають URL у vacancies.md). Усі 24 тести пройдено.

---

## 2026-04-26 03:03

### Додано фільтр за типом компанії з кольоровими суб-фільтрами

**Проблема:** Тип компанії (Product, Outsource, Agency, Startup, Gambling) — один з ключових критеріїв при виборі вакансії, але не було способу швидко відфільтрувати список лише за типом. Кольорове кодування типів існувало на тегах карток, але для фільтрації доводилось вручну вводити назву типу в пошуковий рядок. При 200+ вакансіях це неефективно — продуктові компанії зазвичай найбільш бажані, і мати можливість миттєво показати лише їх — критично для швидкого пошуку.

**Рішення:** Додано повноцінну систему фільтрації за типом компанії: 1) **Кнопка «Type» у панелі фільтрів** — нова кнопка між «$ Salary» та «Pipeline» з лічильником вакансій, що мають визначений тип компанії. Кнопка підсвічується при активному будь-якому type-суб-фільтрі. 2) **Панель суб-фільтрів** — горизонтальний рядок pill-кнопок під фільтрами (подібно до pipeline-воронки), що з'являється при активації будь-якого type-фільтру. Кожна кнопка має кольорову крапку-індикатор та кількість вакансій: Product (зелений), Outsource (жовтий), Agency (фіолетовий), Startup (блакитний), Gambling (червоний). Натискання на чіп встановлює суб-фільтр `type:product`, `type:outsource` тощо. 3) **Рефакторинг `typeClass()`** — виокремлено допоміжну функцію `typeCategory()`, що повертає категорію типу (`product`, `outsource`, `agency`, `startup`, `gambling`), а `typeClass()` використовує її для повернення CSS-класу. Це усуває дублювання regex-логіки між фільтрацією та рендерингом. 4) **Фільтрація `passFilter()`** — розширено для підтримки `type` (всі вакансії з визначеним типом) та `type:CATEGORY` (конкретний тип). 5) **Інтеграція з bottom-nav** — кнопка Type додана до секундарного меню мобільної навігації з коректним підрахунком та підсвіткою.

**Змінені файли:** index.html

**Перевірка:** Імпорт app.py — без помилок. HTML-структура валідна (127003 символів, 1 script open/close). JS-баланс: фігурні дужки 0, круглі 0, квадратні 0. Функція `typeCategory()` коректно категоризує типи через regex. Функція `renderTypeBar()` інтегрована з `renderFilters()` та `setFilter()`. CSS-класи `.type-bar`, `.tb-chip`, `.tb-dot`, `.tb-count` визначено. HTML-елемент `#typeBar` додано між `#filtersWrap` та `#bottomNav`. Фільтрація `type` та `type:CATEGORY` коректно працює в `passFilter()`. Усі 24 тести пройдено.

---

